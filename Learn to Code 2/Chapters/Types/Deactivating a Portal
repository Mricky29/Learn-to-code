greenPortal.isActive = false

for i in 1 ... 1 {

    
    while !isBlocked{
        moveForward()
        
       if !isBlockedLeft{
           turnLeft()
           
       }
        if isOnClosedSwitch{
            toggleSwitch()
            turnLeft()
            turnLeft()
            
        }
        
        
    }